<Window x:Class="MiserlyMiser.Views.Windows.CurrencyWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MiserlyMiser.Views.Windows"
        mc:Ignorable="d"
        Title="CurrencyWindow" Height="450" Width="363"
        FontSize="14">
    <DockPanel>
        <StatusBar DockPanel.Dock="Bottom">
            <StatusBarItem Content="Bin"/>
        </StatusBar>
        <Grid Margin="5, 0">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <TextBlock Text="Выберите валюту" HorizontalAlignment="Center" Margin="0, 15" FontSize="16" Grid.Row="0"/>
            <TextBlock Text="Название" Grid.Row="1" Margin="0, 5"/>
            <ListBox Grid.Row="2">
                
            </ListBox>
            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0, 5">
                <Button Content="Отменить" Margin="5, 0" Width="150"/>
                <Button Content="Выбрать" Width="150"/>
            </StackPanel>
        </Grid>
    </DockPanel>
</Window>
